name: Crommerer
markdown: kramdown
include:
 - _pages/qrcode
prose:
  metadata:
    _posts:
      - name: "date"
        field:
          element: "text"
          label: "Date"
          help: "The date of the event"
          value: CURRENT_DATETIME
          type: "datetime"
